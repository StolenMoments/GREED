{
  "scripts": {
    "dev": "concurrently -n back,front -c cyan,magenta \"uvicorn backend.main:app --reload\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
